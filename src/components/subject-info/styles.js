const styles = theme => ({
  header: {
    backgroundColor: theme.palette.primary.main,
    color: 'white',
  },
  closeIcon: {
    color: 'white',
  },
  inlineIcon: {
    verticalAlign: 'text-bottom',
  },
});

export { styles };
