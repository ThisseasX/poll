const userId = ({ app: { userId } }) => userId;
const pollId = ({ app: { pollId } }) => pollId;
const pollSubject = ({ app: { pollSubject } }) => pollSubject;

export { userId, pollId, pollSubject };
